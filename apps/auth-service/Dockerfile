# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @myapp/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t myapp-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system myapp-auth-service && \
          adduser --system -G myapp-auth-service myapp-auth-service

COPY apps/auth-service/dist myapp-auth-service/
COPY apps/auth-service/package.json myapp-auth-service/
RUN chown -R myapp-auth-service:myapp-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix myapp-auth-service --omit=dev -f install

CMD [ "node", "myapp-auth-service" ]
